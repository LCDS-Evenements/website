import type { ReactElement } from "react";

const SponsorsSection = (): ReactElement => {
  return (
    <section id="sponsors" className="w-screen h-[50dvh] bg-blueWaves bg-no-repeat bg-cover bg-center">
      SponsorsSection
    </section>
  );
};

export default SponsorsSection;